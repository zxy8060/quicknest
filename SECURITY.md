# 安全说明

## 支持范围

安全修复以当前 `main` 分支和最新发布版本为主。

## 报告问题

请优先通过 GitHub Security Advisory 私下报告安全问题，不要在公开 Issue 中粘贴凭据、个人路径、真实 `launcher.json` 或可利用细节。

报告应尽量包含：

- 受影响版本；
- 复现步骤；
- 预期与实际结果；
- 对本地文件、进程启动或权限边界的影响；
- 可安全分享的最小示例。

## 安全边界

- QuickNest 会启动用户配置的本地目标，配置文件本身应视为可信的本地输入。
- 注册表扫描只读取应用路径，不修改注册表业务数据。
- 批量清理只移除 QuickNest 快捷项记录，不删除目标文件。
- 图标数据和本地路径会写入 `launcher.json`，备份或分享前应检查隐私信息。
- 未签名构建可能触发 Windows SmartScreen，这不等同于恶意软件判定。

## 凭据

仓库不需要运行时密钥。请勿提交 GitHub Token、API Key、密码或真实用户数据。
