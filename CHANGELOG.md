# Changelog

本项目遵循面向用户的语义化版本记录。

## [Unreleased]

## [0.4.7] - 2026-09-06

### Fixed

- 兼容旧快捷项将 EXE 文件误存为工作目录的情况，启动时自动取其所在文件夹，修复目标存在却无法启动的问题。
- 无效工作目录给出明确提示，保留用户指定的有效工作目录。

## [0.4.6] - 2026-09-06

### Added

- 已安装应用成功启动后尝试提取图标，同步显示到搜索、已安装应用和最近常用页面。
- 启动及刷新注册表后，为已有使用记录的应用后台补齐图标；成功结果在运行期间缓存，失败可在下次打开或扫描时重试。
- 系统图标提取移入后台线程池，避免阻塞窗口响应。

## [0.4.5] - 2026-09-05

### Changed

- 启动、快捷键、托盘和单实例唤起时，窗口跟随鼠标所在屏幕；跨屏时按目标屏幕缩放比例定位到工作区中央。
- 最小化按钮改为收起到托盘，修复原窗口 API 缺少权限导致无响应的问题。
- 已安装应用的成功启动记录独立持久化，可进入“最近常用”，注册表刷新后不会丢失使用次数。
- 常用列表仅在进程启动时重新排序；运行中首次使用的项目追加到末尾，已有项目实时记录但保持位置稳定。
- 数据 schema 升为 8，为旧数据自动补齐空的已安装应用使用记录。

## [0.4.4] - 2026-08-12

### Fixed

- Windows 无控制台模式下启动部分 Qt 等桌面程序时，不再因继承无效标准句柄而报“句柄无效”。

## [0.4.3] - 2026-08-11

### Added

- 增加默认进入的“最近常用”标签页，按使用次数与最近启动时间综合排序。
- 启动、托盘或全局快捷键唤起 QuickNest 时自动回到“最近常用”。

## [0.4.2] - 2026-07-30

### Added

- 增加“最近使用”页面和搜索框键盘选择。
- 搜索覆盖全部自定义分组和注册表应用；输入时会立即触发未完成的注册表扫描，启动结果后自动清空搜索。
- 每个一级标签独立记住最后停留的汇总页或二级标签。
- 增加当前页面快捷项的批量移动、收藏、取消收藏和移除。
- 增加单实例运行，再次启动时唤起现有窗口。
- 增加版本化数据迁移与迁移 fixture 测试。

### Reliability

- 配置改为临时文件同步写入、有效备份和原子替换。
- 保存请求串行化，托盘退出前等待保存队列完成。
- 设置保存改为事务式应用，失败时恢复快捷键和开机启动状态。
- 首屏不再等待注册表扫描、目标检测或图标补全。

### Security

- 为 Tauri WebView 启用显式 Content Security Policy。
- 增加只读权限的 Windows CI 和 Dependabot 更新检查。

### Documentation

- 增加第三方依赖许可证清单、支持入口、行为准则和 GitHub 社区模板。
- 明确 QuickNest 与设计参考项目不存在官方关联。

### Changed

- 使用 QuickNest 图标替换未使用的 Vite 模板图标，并移除未引用的脚手架资源。
- 快捷项改为单击选中、双击启动；方向键与回车启动保持不变。
- 启动快捷项后保持 QuickNest 界面可见，不再提供“启动项目后隐藏”设置。
- 主窗口不再显示在 Windows 任务栏，托盘入口保持可用。
- 窗口位于后台时，全局快捷键优先将其带到前台。

## [0.4.1] - 2026-07-27

### Added

- 当前页面失效快捷项批量清理。
- 默认全选的逐项复选清单。
- 删除前第二层最终确认。

### Safety

- 清理流程排除注册表应用。
- 清理只移除 QuickNest 记录，不删除目标文件。

## [0.4.0] - 2026-07-27

### Added

- 后台异步目标存在性检测。
- 失效目标红色状态。
- 一级标签自动进入首个二级标签。
- 重启恢复上次浏览位置。

### Changed

- 启动项图标和文字改为居中排列。

## Earlier releases

早期版本完成了 Lucy 数据迁移适配、两级分组、注册表应用汇总、可配置图标尺寸、可拖动无边框窗口、固定设置保存栏和录制式全局快捷键。
